apiVersion: v1
kind: Service
metadata:
  name: weather-app-service
  namespace: secure-ns
spec:
  selector:
    app: weather-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
